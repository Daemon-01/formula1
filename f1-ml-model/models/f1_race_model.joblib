import joblib 
import os

model_path = os.path.join("models","f1_race_model.joblib")
joblib.dump(search.best_estimator_,model_path)
print(f"Best model after hyper-parameter tuning is saved: {model_path}")

early_model_path = os.path.join("models","f1-race_early_model")
joblib.dump(xgb_early,early_model_path)
print(f"Model before tuning is saved: {early_model_path}")

print("Files created:", [f for f in os.listdir('models') if f.endswith('.joblib')])